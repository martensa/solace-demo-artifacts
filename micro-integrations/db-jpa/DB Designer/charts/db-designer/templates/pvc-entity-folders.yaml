{{- if .Values.servicesApp.entityFolders.persistence.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "db-designer.fullname" . }}-entity-folders
  labels:
    {{- include "db-designer.labels" . | nindent 4 }}
    app.kubernetes.io/component: services
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.servicesApp.entityFolders.persistence.storageClass }}
  storageClassName: {{ .Values.servicesApp.entityFolders.persistence.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.servicesApp.entityFolders.persistence.size | quote }}
{{- end }}
